<%= simple_form_for(@status, html: { class: 'form-horizontal' }) do |f| %>
<fieldset>
    <% if @status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@status.errors.count, "error") %> prohibited this status from being saved:</h2>

      <ul>
      <% @status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.input :user_id, collection: User.all, label_method: :nombre_completo %>
  <%= f.input :contenido,placeholder: 'Tu post aquí' %>
  <div class="form-actions">
    <%= f.button :submit %>
  </div>
</fieldset>
<% end %>

<!-- en el input de user_id llamamos el metodo collection proporcionado x simple form y lo llenamos
con el modelo de usuario q nos trae todos los usuarios con el campo nombre_completo -->